# Azure AD App Registration Credentials
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git!

# From Azure Portal > App Registrations > Your App > Overview
CLIENT_ID=your-client-id-here
TENANT_ID=your-tenant-id-here

# Optional: Only needed if you want to use client secret authentication
# From Azure Portal > App Registrations > Your App > Certificates & secrets
CLIENT_SECRET=your-client-secret-here

# Microsoft Graph API Configuration
GRAPH_API_ENDPOINT=https://graph.microsoft.com/v1.0
AUTHORITY=https://login.microsoftonline.com/common

# Scopes (permissions) needed for the application
SCOPES=Files.Read.All User.Read

# Local storage paths
DOWNLOAD_DIR=downloaded_files
EXTRACTED_TEXT_DIR=extracted_text
LOGS_DIR=logs

# File types to process
SUPPORTED_FILE_TYPES=docx,pdf

# Pagination settings
MAX_ITEMS_PER_PAGE=200

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
